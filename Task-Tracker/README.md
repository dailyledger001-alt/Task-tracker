# Task Tracker

A standalone, local-first Task Tracker extracted from Daily Ledger. Variable tasks, recurring rules and tasks, habits, and the daily journal stay connected in one product.

Open `index.html` directly in a modern browser. Your data stays in that browser's local storage. Use the top-right backup menu before clearing browser data or moving devices.

For the automated smoke test:

```bash
npm install
npm test
```
